// import { fromJS } from 'immutable';
// import { makeSelectDonationPageDomain } from '../selectors';

// const selector = makeSelectDonationPageDomain();

describe('makeSelectDonationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
