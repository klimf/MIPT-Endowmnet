// import { fromJS } from 'immutable';
// import { makeSelectCapitalPageDomain } from '../selectors';

// const selector = makeSelectCapitalPageDomain();

describe('makeSelectCapitalPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
