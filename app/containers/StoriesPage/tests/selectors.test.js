// import { fromJS } from 'immutable';
// import { makeSelectStoriesPageDomain } from '../selectors';

// const selector = makeSelectStoriesPageDomain();

describe('makeSelectStoriesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
