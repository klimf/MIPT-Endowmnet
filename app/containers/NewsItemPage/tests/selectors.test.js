// import { fromJS } from 'immutable';
// import { makeSelectNewsItemPageDomain } from '../selectors';

// const selector = makeSelectNewsItemPageDomain();

describe('makeSelectNewsItemPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
