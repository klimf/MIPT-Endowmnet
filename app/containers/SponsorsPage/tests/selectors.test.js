// import { fromJS } from 'immutable';
// import { makeSelectSponsorsPageDomain } from '../selectors';

// const selector = makeSelectSponsorsPageDomain();

describe('makeSelectSponsorsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
