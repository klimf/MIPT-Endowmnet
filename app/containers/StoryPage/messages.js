/*
 * StoryPage Messages
 *
 * This contains all the text for the StoryPage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.StoryPage.header',
    defaultMessage: 'This is StoryPage container !',
  },
});
