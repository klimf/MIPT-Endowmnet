// import { fromJS } from 'immutable';
// import { makeSelectStoryPageDomain } from '../selectors';

// const selector = makeSelectStoryPageDomain();

describe('makeSelectStoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
