// import { fromJS } from 'immutable';
// import { makeSelectGenericPageDomain } from '../selectors';

// const selector = makeSelectGenericPageDomain();

describe('makeSelectGenericPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
