// import { fromJS } from 'immutable';
// import { makeSelectCompaniesPageDomain } from '../selectors';

// const selector = makeSelectCompaniesPageDomain();

describe('makeSelectCompaniesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
