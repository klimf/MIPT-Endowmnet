// import { fromJS } from 'immutable';
// import { makeSelectForbiddenPageDomain } from '../selectors';

// const selector = makeSelectForbiddenPageDomain();

describe('makeSelectForbiddenPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
