// import { fromJS } from 'immutable';
// import { makeSelectAboutPageDomain } from '../selectors';

// const selector = makeSelectAboutPageDomain();

describe('makeSelectAboutPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
