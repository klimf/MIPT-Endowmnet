// import { fromJS } from 'immutable';
// import { makeSelectNewsPageDomain } from '../selectors';

// const selector = makeSelectNewsPageDomain();

describe('makeSelectNewsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
