// import { fromJS } from 'immutable';
// import { makeSelectCapitalsPageDomain } from '../selectors';

// const selector = makeSelectCapitalsPageDomain();

describe('makeSelectCapitalsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
